{
  "name": "electron-package",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager ./ vue --platform=win32,darwin --arch=all --version=1.0.0 --out=./dist --overwrite --ignore=node_modules --ignore=.gitignore --prune"
  },
  "author": "lyric",
  "license": "ISC",
  "dependencies": {
    "electron": "^1.6.10",
    "electron-packager": "^8.7.1"
  }
}
